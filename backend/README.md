# HomeBudget Backend
Django REST API для управления личными и семейными финансами
## 🛠 Технологии
- **Python 3.11+** — язык программирования
- **Django 4.2+** — веб-фреймворк
- **Django REST Framework** — REST API
- **PostgreSQL 15+** — база данных
- **JWT** — аутентификация через токены
- **Celery** — асинхронные задачи
- **Redis** — кеширование и очередь задач
- **Pillow** — обработка изображений
- **Pandas** — работа с CSV/Excel файлами
- **ReportLab** — генерация PDF отчётов

## Требования
- **Python** 3.11 или выше
- **PostgreSQL** 15 или выше
- **Redis**

## Установка
### 1 Создание виртуального окружения
```bash
# Перейти в папку backend
cd backend

# Создать виртуальное окружение
python -m venv venv

# Активировать окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate
```
### 2 Установка зависимостей
```bash
# Установить все зависимости
pip install -r requirements/base.txt
```
### 3 Настройка базы данных
```bash
# Django
SECRET_KEY=django-insecure-ваш secret key
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Database 
DB_NAME=homebudget
DB_USER=postgres
DB_PASSWORD= ваш пароль (при установке postgreSQL)
DB_HOST=localhost
DB_PORT=5432 (если вы не меняли при установка postgreSQL)
```
Установка будет со временем обновляться (я надеюсь)

### 4 Запуск сервера
```bash
python manage.py runserver
```